extends structure/_layout.pug

block vars
  - pageKey = home
  - bodyClass = pageKey.body_class
  - siteTitle = pageKey.title
  - siteDescription = pageKey.description
  - siteKeywords = pageKey.keywords

block content
  aside(style="background-image: url('static/cover.jpg');")
